# P2 平台、日期规则与聚合验证记录

## 已实现

- JSON 化通用阈值、评分权重及淘宝、天猫、京东、拼多多、抖音平台规则。
- 平台正向词、负向词、最低得分和近分歧义判定；平台结果只作辅助展示、订单号提取和排查，不作为日期审核门槛。
- `YYYY-MM-DD`、`YYYY/MM/DD`、`YYYY.MM.DD`、中文年月日及可选时分秒解析。
- 日期正则跨最多三个相邻 OCR 块，并只保留真正与命中字符重叠的来源块。
- 强标签、弱标签、排除标签、同行右侧、正下方及 OCR 置信度评分。
- 订单号提取、候选近分冲突、晚于报销日期复核和可解释评分证据。
- 按 BX、订单号、日期及内容哈希进行多附件聚合，显式标记重复附件。
- 提取结果按规则文件哈希和规则引擎版本写入 SQLite；第二次运行无需重新执行规则。

## 当前业务数据试算

- 文件结果：162 个 `AUTO_ACCEPTED`、2 个 `CONFLICT`、50 个 `NO_ORDER_DATE`、2 个 `DUPLICATE`，无仅因分数落入中间区间产生的 `NEEDS_REVIEW`。
- 平台分布：京东 8、拼多多 2、淘宝 46、天猫 56、未知 104。
- BX 聚合结果：160 个 `AUTO_ACCEPTED`、4 个 `NEEDS_REVIEW`、1 个 `CONFLICT`、39 个 `NO_ORDER_DATE`。
- OCR 缓存缺失：0。
- 第二次 P2 运行：216/216 命中规则版本缓存。

这些数字表示规则覆盖和保守程度，不代表准确率。未经过人工标签前，不得宣称达到 99% 自动通过准确率。

## 黄金样本校准门槛

已在 Git 仓库外生成 100 条分层黄金样本工作簿，覆盖平台与状态组合，包含预测结果、规则证据及人工标注列。工作簿含业务隐私，不进入 Git。

后续必须完成：

1. 人工填写平台、订单号、下单时间和结论。
2. 锁定 30% 验证集，调规则期间不得查看验证集结果。
3. 只在开发集调整关键词、空间阈值和评分。
4. 自动通过准确率达到 99% 后锁定规则版本；未达到时继续 P2，不进入正式 P3 验收。

## 日期级人工校准结果

业务验收口径已明确为只比较 `YYYY-MM-DD`，不比较时分秒。验证工具直接读取规则结果 JSON 中的本地日期，避免工作簿展示层的时区转换影响结论。

- 100 条黄金样本中，整体日期或无日期判断正确 99 条（99.00%）。
- 96 条有人工日期的样本中，年月日正确 95 条（98.96%）。
- 4 条无人工日期的样本均正确保持无日期。
- 37 条 `AUTO_ACCEPTED` 样本年月日全部正确（100.00%），达到自动通过准确率门槛。
- 唯一剩余错误处于 `NO_ORDER_DATE`，不会静默进入正式结果。

规则同步调整为按自然日判断候选冲突和多附件一致性；同一天不同时刻不再互相冲突。与“下单时间/订单创建时间”同行的候选视为权威下单日期，不被上一行支付时间误伤；同时新增期望配送/预计送达排除标签。普通“创建时间”仍保留负向邻近检查，避免可疑页面被放宽为自动通过。

可重复验证命令：

```powershell
python order_date_golden_cli.py --predictions <规则结果.json> --workbook <人工标注.xlsx>
```

命令退出码以自动通过样本日期准确率是否达到默认 99% 为准；比较时只使用年月日，人工列中的无日期文字说明按无日期处理。

## P2 状态

规则实现、缓存、人工标注和日期级校准已完成；当前黄金样本上的 `AUTO_ACCEPTED` 日期准确率达到 100%，满足 99% 门槛。进入 P3 前仍应另行锁定未参与调规则的验证集，避免把本次校准集成绩误当作独立泛化结果。

## 空间归属修复

P4 全量检查发现，纵向时间列表中与“创建时间”同行的日期会被上方付款、发货或成交标签错误扣分。规则引擎版本 4 调整为：正向标签与日期同行时，其他行仅通过宽松“下方”窗口命中的排除标签不再作用于该日期；真正同行的付款等排除标签仍保留扣分。

复核过程中同时确认原黄金样本有一条人工日期误取成交时间，已在仓库外新副本中改为创建日期，原文件未覆盖。修复并更正标签后，100 条样本整体日期、96 条有日期样本、4 条无日期样本以及 51 条自动通过样本均达到 100% 日期一致率。

## 平台中立判定

规则引擎版本 5 移除已知平台加分和未知平台扣分，并将原平台加分并入完整日期的基础证据。日期评分函数不再接收平台或平台规则参数，所有平台统一使用通用日期标签和排除标签。因此，相同日期内容在已知平台和未知平台上的得分及状态完全一致。平台识别仍保留在明细中，仅用于展示、订单号辅助和排查页面来源。

平台中立规则对 216 个附件重算后，文件级自动通过由 87 个增至 147 个；100 条黄金样本整体一致率保持 100%，其中 78 条自动通过样本按年月日比较全部正确。

## 标准订单时间线

订单详情同时显示创建、付款、发货和成交时间属于正常页面结构。只要完整日期与“创建时间”或“购买时间”同行，该标签即拥有该日期；同页或相邻行出现的付款、发货、成交标签不得反向扣分。只有日期本身同行的是付款等排除标签、且没有同行创建/下单标签时，才拒绝将其作为下单日期。

修复后文件级自动通过由 147 个增至 162 个，待处理由 67 个降至 52 个。100 条黄金样本整体一致率保持 100%，其中 86 条自动通过样本全部正确。
