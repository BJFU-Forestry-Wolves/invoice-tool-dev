# 发票开票公司 OCR 与单日限额核验

## 核验口径

- OCR 目标是发票的“销售方名称”，不是购买方名称。
- 以订单日期为日期口径；日期来自现有订单日期识别/人工确认结果。
- 按“订单日期 + 规范化销售方名称”分组。
- 每个报销编号的金额只累计一次，避免一条报销记录附有多张同公司发票时重复计算。
- 当日同公司合计 **大于** 1000 元标记为 `EXCEEDED`；等于 1000 元允许通过。
- 销售方不确定、订单日期缺失、同一报销编号存在多个销售方、金额缺失或冲突时进入人工复核。

## 隐私隔离

发票、OCR 缓存和运行结果必须位于 Git 仓库外。工作流会拒绝把结果目录或缓存文件写入仓库。

## 运行

```powershell
.\.ocr-venv\Scripts\python.exe invoice_issuer_cli.py `
  --manifest "<仓库外分类目录>\classification_manifest.json" `
  --attach-root "<仓库外附件根目录>" `
  --output-dir "<仓库外结果目录>\invoice-issuer"
```

先加 `--limit 3` 做小样本验证。OCR 结果使用 SQLite 缓存，后续重复运行会跳过已识别的文件。
