# P1 输入、OCR 与缓存主链路验证记录

## 已实现

- 共享 CSV 记录加载和上传人规范化，旧附件整理脚本复用同一实现。
- BX 文件精确匹配、递归扫描、扩展名校验及流式 SHA-256。
- PaddleOCR 延迟初始化单例封装，启动前校验模型文件和哈希。
- PaddleOCR 3.7 的 `{"res": ...}` 结果信封会先解包，再转换为版本无关的 `OCRPage` / `OCRTextBlock` 对象。
- SQLite WAL 缓存，按文件哈希和 OCR 引擎指纹进行版本失效。
- 成功和失败状态均按文件事务提交，可在中断后继续运行。
- P1 命令行支持 CSV/BX 过滤、强制重跑、扫描模式、运行摘要和安静模式。
- 缓存和运行摘要强制保存在 Git 仓库之外。

## 本机全量验证

- 订单附件扫描：216 个可处理文件，0 个格式或命名问题。
- 初次结构验证发现 PaddleOCR 3.7 JSON 外层存在 `res` 信封；适配器版本已加入缓存指纹，旧的空块缓存会自动失效并重建。
- 修正后全量主链路：213 个新 OCR 成功，3 个按相同内容哈希复用缓存，0 个失败。
- 214 个唯一内容页共写入 14,867 个文字块；空文字页为 0，单页最少 27 块、最多 134 块。
- 首次全量结束后：216 个附件全部具有明确且非空的 OCR 状态。
- 第二次全量运行：216 个全部 `SKIPPED_CACHE`，没有初始化 OCR 模型。
- 自动化测试覆盖 CSV、BX 匹配、格式校验、哈希、OCR 标准化、成功缓存、失败缓存和引擎版本失效。

真实 SQLite 缓存、运行摘要、附件路径和 OCR 文本均保存在仓库外的本地测试数据目录，不进入 Git。

## CLI 示例

以下参数均使用占位路径；真实业务路径不得写入仓库文档或脚本：

```powershell
.\.ocr-venv\Scripts\python.exe .\order_date_cli.py `
  --attach-root <仓库外附件根目录> `
  --cache <仓库外缓存文件> `
  --summary <仓库外运行摘要> `
  --quiet
```

## P1 状态

P1 本机退出条件已重新满足：现有订单附件全部得到非空 OCR 状态，二次运行只处理变化文件。可进入 P2 平台检测、日期候选提取、规则评分与多附件聚合。
