# P4 全量验证报告

## 结论

已对当前全部 216 个订单附件执行 P4 全量缓存回归，未出现 OCR 失败、缓存缺失、文件扫描跳过或 Excel 写入错误。四张工作表通过字段、公式和视觉检查。平台识别已从日期评分和审核门槛中完全移除；平台字段仅保留为辅助展示、订单号提取和排查。100 条人工标注样本按年月日比较的整体一致率和自动通过一致率均为 100%。

当前结果满足全量技术回归要求，但尚不能宣称完成发布验收：原始业务 CSV 不在工作区，本次只能使用 201 个唯一 BX 编号生成验证专用清单；完整集合中另有 69 个自动通过附件不在现有黄金样本内，且尚未完成干净 Windows 测试机与发布包验证。

## 全量运行结果

| 指标 | 结果 |
|---|---:|
| 订单附件 | 216 |
| 唯一 BX 编号 | 201 |
| 聚合订单结果 | 204 |
| OCR 缓存命中 | 216 / 216 |
| 规则缓存命中 | 216 / 216 |
| OCR 失败 | 0 |
| 扫描跳过 | 0 |
| 工作流耗时 | 0.967 秒 |

文件级状态：自动通过 147、未确认下单日期 65、冲突 2、重复附件 2。聚合后自动通过 145、待复核 4、未确认下单日期 54、冲突 1。

自动通过覆盖率为 68.06%（147 / 216）；聚合自动通过覆盖率为 71.08%（145 / 204）。文件级待人工处理由平台中立调整前的 125 个降至 67 个（65 个无确认日期、2 个冲突），减少 58 个；相对最初的 152 个减少 85 个。系统仍会拦截日期缺失、排除标签命中和候选冲突。

## 平台与格式

平台分布：京东 8、拼多多 2、淘宝 46、天猫 56、未知 104。平台识别覆盖率不再作为日期验收指标，未知平台不会降低日期分数或触发复核。

BMP、JPEG、JPG、PDF、PNG 五种格式均成功读取，格式级成功率均为 100%。本批 PDF 均为单页，因此总页数与文件数同为 216。

## 人工标注准确率

验收口径只比较年月日，不要求时分秒一致：

| 样本集合 | 正确数 | 总数 | 准确率 |
|---|---:|---:|---:|
| 全部人工标注 | 100 | 100 | 100.00% |
| 有下单日期 | 96 | 96 | 100.00% |
| 人工确认无日期 | 4 | 4 | 100.00% |
| 自动通过 | 78 | 78 | 100.00% |

复核发现原人工标注中有一条把成交时间误写为下单日期，已在不覆盖原文件的更正副本中改为创建日期。完整集合共有 147 个自动通过附件，其中 78 个已包含在黄金样本中，剩余 69 个仍需在发布前独立抽检或纳入锁定验证集。

## Excel 验证

- 工作表范围：汇总 205 行、附件明细 217 行、人工复核 68 行、运行统计 24 行。
- 下单日期与下单时间为真实 Excel 日期值。
- 订单号保持文本显示，不使用科学计数法。
- 源附件超链接、筛选、冻结表头、状态颜色和人工复核输入区正常。
- 公式错误扫描匹配数为 0。
- 四张工作表均完成代表性区域渲染检查，无严重裁切、重叠或不可读内容。

## 隐私与数据边界

- 完整附件、人工标注、验证 CSV、SQLite 缓存、JSON 指标和 Excel 报告均位于 Git 仓库外。
- 新增 P4 CLI 只把扫描摘要中的 BX 编号写入仓库外验证清单，其他业务字段使用明确的验证占位值。
- 仓库内报告不记录真实 BX 编号、附件路径或人工标注明细。

## 发布前剩余事项

1. 获取原始业务 CSV，重新生成含真实上传人、用途和金额的最终业务报告。
2. 独立复核当前黄金样本外的 69 个自动通过附件。
3. 在干净 Windows 10/11 x64 机器验证启动、模型定位、离线运行和 Excel 超链接。
4. 完成 PyInstaller `--onedir` 发布包、第三方许可证清单和版本号锁定。
